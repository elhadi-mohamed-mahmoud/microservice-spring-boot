server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/elhadi-mohamed-mahmoud/ms-config-server
          clone-on-start: true
          default-label: main
